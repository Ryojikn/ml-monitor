# Backend
DATABASE_URL=sqlite+aiosqlite:///./mlmonitor.db
UPLOAD_DIR=./uploads
CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]

# Frontend (Vite)
# When running without Docker, the Vite proxy handles /api → localhost:8000
# Set this only if your backend runs on a different host
# VITE_API_URL=http://localhost:8000
